<ul>
  <% @memos.each do |memo| %>
    <li><a href="<%= "/memos/#{memo[:id]}" %>"><%= html_escape(memo[:title]) %></a></li>
  <% end %>
</ul>
<form action="/memos/create" method="GET">
  <input type="submit" value="追加">
</form>
