<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="utf-8">
<title>メモアプリ</title>
</head>

<body>
  <h1>メモアプリ</h1>
  <ul>
    <% @memos.each do |memo| %>
      <a href=<%= "/memos/#{memo[:id]}"%>><%= html_escape(memo[:title]) %></a><br />
    <% end %>
  </ul>
  <form action="<%="/memos/create"%>" method="GET">
    <input type="submit" value="追加">
  </form>
</body>
</html>
